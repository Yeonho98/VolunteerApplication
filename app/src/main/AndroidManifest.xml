<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ui_frame">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UI_Frame">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivityPrivate" />
        <activity android:name=".MainActivityPublic" />
        <activity android:name=".MainActivityContract" />
        <activity android:name=".MainActivityDBM" />
        <activity android:name=".MainCategoryActivity1" />
        <activity android:name=".MainCategoryActivity2" />
        <activity android:name=".MainMyPageActivity" />
        <activity android:name=".CategoryOrganizationList" />
        <activity android:name=".IndividualOrganization" />
        <activity android:name=".IndividualOrganizationSub" />

    </application>

</manifest>